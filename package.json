{
  "name": "visual-pomodoro",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "electron:dev": "node scripts/dev-electron.mjs",
    "electron:build": "vite build && npx @electron/packager . \"Visual Pomodoro\" --out release --overwrite --platform=win32 --arch=x64"
  },
  "dependencies": {
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  },
  "devDependencies": {
    "@electron/packager": "^20.0.0",
    "@tailwindcss/vite": "^4.2.4",
    "@vitejs/plugin-react": "^6.0.1",
    "electron": "^36.0.0",
    "electron-builder": "^26.8.1",
    "tailwindcss": "^4.2.4",
    "vite": "^8.0.11"
  }
}
